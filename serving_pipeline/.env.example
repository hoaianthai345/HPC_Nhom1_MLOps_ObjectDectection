# Server Configuration
DEBUG=false
HOST=0.0.0.0
API_PORT=8000
GRADIO_PORT=7860
API_HOST=localhost  # Use 'api' when running in Docker compose

# Image Validation
MAX_FILE_SIZE_MB=10.0
MIN_IMAGE_WIDTH=32
MIN_IMAGE_HEIGHT=32
MAX_IMAGE_WIDTH=4096
MAX_IMAGE_HEIGHT=4096

# YOLO Model Configuration
YOLO_MODEL_PATH=yolov8n.pt
YOLO_CONFIDENCE_THRESHOLD=0.25
YOLO_IOU_THRESHOLD=0.45
YOLO_MAX_DETECTIONS=100

# Production Data Storage
PRODUCTION_DIR=./production

# MLflow Integration (if using model registry)
MLFLOW_TRACKING_URI=http://localhost:5000
MLFLOW_MODEL_URI=models:/yolo-student/Production

# MinIO/S3 Credentials (if needed for artifact storage)
AWS_ACCESS_KEY_ID=minio_admin
AWS_SECRET_ACCESS_KEY=minio_password123
MLFLOW_S3_ENDPOINT_URL=http://localhost:9000

# Monitoring
PROMETHEUS_ENABLED=true
